#include <bits/stdc++.h>
using namespace std;

int main(){
	int A, B, K; cin >> A >> B >> K;
	string a, b, S;
	for(int i = 0; i < A; i++) a += "a";
	for(int i = 0; i < B; i++) b += "b";
	S = a + b;
	//for(int i = 0; i < S; i++){
		//sort(S.be)

	cout << S << endl;
}

